import React from "react";
import Defaultpage from "./components/Defaultpage";

// Includes all the code we want to render.
function App(): JSX.Element {
    return (
        <div>
            <Defaultpage />
        </div>
    );
}

export default App;
